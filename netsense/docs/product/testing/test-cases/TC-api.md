# Test Cases — API

1. Verify the health endpoint returns `healthy` when services are operational.
2. Verify device list API returns discovered devices.
3. Verify incident creation and retrieval endpoints work as expected.
4. Verify authentication and role-based access control for admin-only routes.
5. Verify PCAP download endpoint requires senior role.

