# Test Cases — Normalisation

1. Verify protocol events are normalised into standard event schema.
2. Verify timestamp correction is applied for request/response pairs.
3. Verify unsynced clock events are tagged when uncertainty exceeds 50ms.
4. Verify raw packet data is transformed into device/metric events.
5. Verify normalised events are accepted by the platform API.

