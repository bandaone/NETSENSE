# Dashboard Guide

## Blast Radius View

In Critical Incident mode, the topology map should show the blast radius from the active fault:

- Fade unaffected nodes and links.
- Highlight the affected downstream path in amber or another high-contrast warning color.
- Keep the physical topology visible so engineers can see what is breaking without switching to a table view.
